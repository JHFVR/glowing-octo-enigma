---
applications:
- name: Chatbot_streamlit
  random-route: true
  path: ./
  memory: 128M
  buildpacks: 
  - python_buildpack
  # command: streamlit run streamlit_app.py --server.port 8080 --server.enableCORS false
  services:
  - streamlit_app_jvr
